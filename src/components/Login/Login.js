import React from 'react';

import LoginReduxForm from './LoginForm';

const Login = () => {
    return (
        <div>
            <h1>LOGIN</h1>
            <LoginReduxForm />
        </div>
    )
};

export default Login;
